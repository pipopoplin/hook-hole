# Core — no dependencies needed for regex_filter hook

# NLP plugins for llm_filter hook (install the one you need)

# spaCy — edge/low-resource, ~3ms
# After install: python -m spacy download en_core_web_sm
spacy>=3.7

# Microsoft Presidio — known PII, ~0.4ms
# presidio-analyzer>=2.2

# GLiNER — zero-shot NER, ~18ms
# gliner>=0.2

# DistilBERT / NerGuard — best accuracy, ~25ms
# transformers>=4.36
# torch>=2.1
