# Hook system — no dependencies needed for regex_filter

# NLP plugins for llm_filter (install the one you need)
# See .claude/hooks/plugins/plugins.json for the full registry

# spaCy — edge/low-resource, ~3ms (default)
# After install: python -m spacy download en_core_web_sm
spacy>=3.7

# Microsoft Presidio — known PII, ~0.4ms
# presidio-analyzer>=2.2

# GLiNER — zero-shot NER, ~18ms
# gliner>=0.2

# DistilBERT / NerGuard — best accuracy, ~25ms
# transformers>=4.36
# torch>=2.1

# Diagram generation (optional, for docs/*)
# npm: npx @mermaid-js/mermaid-cli
